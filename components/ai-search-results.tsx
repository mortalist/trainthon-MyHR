"use client";

// query: 검색창의 trim된 비어있지 않은 문자열. 이름 로컬 매칭 아래에 렌더된다 (3단계에서 /api/ask mode=search 결과 + 근거로 교체).
export function AiSearchResults({ query }: { query: string }) {
  void query;
  return null;
}
