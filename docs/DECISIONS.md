# MyHR — 결정 로그

정본은 [PRODUCT.md](PRODUCT.md). 이 파일은 거기에 없는 것 — **무엇을 버렸고 왜 버렸는지.** 새 결정이 나면 PRODUCT.md를 고치고 여기에 한 줄 남긴다.

| 날짜 | 결정 | 버린 것 | 이유 |
| --- | --- | --- | --- |
| 08-26 | 독립 앱으로 피벗 | 구글 연락처 MCP (Claude Code 툴) | Google Contacts `notes` 필드로는 사람 사이 엣지를 표현 못함. 데모 가능한 프로덕트가 필요 |
| 09-01 | Next.js App Router + Vercel + Supabase Postgres. 클라이언트 스토어 없음 | Vite SPA, React Query/Zustand | LLM 키를 Route Handler에 숨겨야 함. 20시간에 상태 라이브러리는 부채 |
| 09-01 | 홈 = 평범한 연락처 목록 (트로이 목마) | 그래프 캔버스 주 화면 | 노드·엣지는 설명에 시간이 든다. 익숙한 화면에서 불가능한 검색이 되는 것이 서사. 그래프는 상세 「공통 지인」 + 여유 시 토글로 강등 |
| 09-01 | 390px 폰 프레임 고정 | 데스크탑 고정, 반응형 | 목록·FAB·스와이프는 모바일 패턴. 레이아웃 1개가 반응형보다 싸다 |
| 09-01 | 와우 1순위 = 카톡 캡처 + 속성 검색 | 음성 메모 우선 | Web Speech는 Safari/iOS 불안정, 호텔 와이파이 리스크. 비전 호출 1회가 실패 확률 낮고 시각적으로 강렬. 마이크는 UI만, 폴백 텍스트박스는 처음부터 |
| 09-01 | 킬러 질의 = 「주말 축구 11명」 | 「반도체 쪽 소개받을 사람」 | 출력이 눈에 보이는 명단이고 쓰는 상황이 즉시 이해됨. 추상 질의는 심사위원이 대입하는 데 시간이 든다 |
| 09-01 | 입력 모달에 new/existing 선택 단계 없음 | 사람 먼저 고르고 입력 | 엔티티 해소는 AI가 하고 결과를 스와이프 카드에 드러낸다. 선택적 인물 지정 필드는 탈출구로 남김 |
| 09-01 | 엣지는 사람 카드 안에 표시 | 엣지 전용 스와이프 카드 | 그룹방 한 장이 카드 9장이 되면 무대에서 지친다 |
| 09-01 | 승인 없는 자동 저장 없음 | 빠른 저장 모드 | 틀린 키 하나가 다음 키 목록을 오염시킨다. 카드는 「AI Tool 활용도」 심사축을 시각화하는 유일한 지점. 실사용 생략은 Q&A 답변으로 |
| 09-01 | 임베딩/pgvector/RAG 제외 | 2단계 검색 | 시드 30명은 통째로 5~10K 토큰. full-context가 더 정확하고 구현 4시간 → 20분. 100명 넘으면 복귀 ([PITCH.md](PITCH.md) 확장 답변) |
| 09-09 | 사실 저장 = `attributes(person_id, key, value, source)` + 목록용 `one_liner` | ① 사람별 `.md` 파일 ② 컬럼 고정 표 ③ `dossier` 마크다운 컬럼 | ① Vercel은 영속 FS 없음, 필터마다 전체 파싱 ② 새 특성마다 `ALTER TABLE`, 나머지 전원 NULL ③ 매 추출마다 전문 재작성 → 유실, 「축구 11명」이 SQL이 아니라 LLM 통독이 됨. 속성 행은 INSERT 한 줄로 스키마가 자란다 |
| 09-09 | 와우 B/C는 SQL 먼저, 매핑 안 되면 LLM | 모든 질의 full-context | `likes_spicy`, `plays_soccer`는 `WHERE` 한 줄. LLM은 양 끝(추출·서술)에만 |
| 09-09 | LLM은 「2~4초 안에 카드」 기준으로 고름. Zod 강제는 유지 | Claude Opus 5 고정 | 무대 체감 속도 > 모델 크기 |
| 09-09 | 상단 필터 = 커뮤니티 태그 (주식동아리·농구동아리·셰플러·트레인톤) | 직장·가족·동아리·행사 | 시드가 커뮤니티 기준으로 짜여 있다 |
| 09-09 | 전화번호 컬럼 없음. 「800명」은 이름만 있는 더미 행 | 연락처 임포트 | 임포트는 페이크. 더미는 프롬프트·속성 쿼리에 안 들어감 |
| 09-09 | 시드 보호 | 참가자·무대 공용 DB 쓰기 | 참가자가 `likes_spicy`를 더럽히면 피칭이 깨진다. 참가자 쓰기는 브라우저 로컬 또는 무대 직전 리셋 |
| 09-09 | 타 제품 브랜드 비교 삭제. 과금 = 무료 한도 + 토큰 | 무료 N명 / Pro 월정액, 직군 웨지 중심 | 적절한 비교 대상 아님. AI Pro처럼 누구나 + 사용량. 확장은 캘린더·온디바이스 |
